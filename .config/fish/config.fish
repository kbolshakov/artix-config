if status is-interactive
    # Commands to run in interactive sessions can go here
    #fastfetch --logo-color-1 blue --color blue  # this is a test
    fish_add_path /usr/local/sbin /usr/local/bin /usr/sbin/ /usr/bin /sbin /bin
end
